---
installer: oracle-skills-cli v2.0.0
name: where-we-are
description: v2.0.0 L-SKLL | Session awareness - alias for /recap --now. Use when user asks "now", "where are we", "what are we doing", "status".
---

# /where-we-are

This skill has been merged into `/recap --now`.

## Instructions

Run `/recap --now` (or `/recap --now deep` if user asked for deep mode).

Pass arguments: `$ARGUMENTS`
